// const joi = require('joi');

// const Joi = require('joi');

// const messageVal = Joi.object().keys({
//     name:Joi.string().min(5).required(),
//     email:Joi.string().lowercase().required(),
//     message:Joi.string().required()
// })

// module.exports = messageVal;